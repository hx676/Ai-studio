# SynCanvas 输出到文件夹

适用于 Classic 和 Smart Canvas 的原生前端扩展节点。节点只在用户点击运行后调用浏览器文件夹选择器，并将连接的图片、音频或视频写入用户明确选择的目录。

- 不向后端提交目标文件夹路径。
- 不保存浏览器目录句柄或绝对路径。
- 默认遇到重名文件时自动追加序号，也可选择覆盖。
- 单次最多 500 个文件；单文件最大 500 MiB；累计最大 1 GiB。

